# Optional on-device AI runtime dependencies.
# These are NOT required to run the base application shell.
# They enable the real local providers (RapidOCR, Vosk) once installed.
# Install with:  pip install -r requirements-ai.txt
rapidocr-onnxruntime>=1.3
vosk>=0.3.45
pyttsx3>=2.90